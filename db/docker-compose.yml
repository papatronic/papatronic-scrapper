version: "3"

services:

  postgresql:
    env_file:
      - ./database.env
    image: bitnami/postgresql:11
    ports:
      - "5432:5432"
    volumes:
      - "/Users/renevidriales/Documents/ITC/papatronic-scrapper/db/data:/bitnami/postgresql"
      - "./schema.sql:/docker-entrypoint-initdb.d/schema.sql"
    networks: 
      - database

networks:
  database:
    driver: bridge
